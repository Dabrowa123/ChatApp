import Box from "@mui/material/Box";
import { useSelector } from "react-redux";
import Message from "../Message";

const ChatSection = () => {
  return (
    <Box sx={{ bgcolor: "#f5f5f5", height: "100vh", width: "60vw" }}></Box>
  );
};

export default ChatSection;
